{% extends 'base.html.twig' %}

{% block title %}Libellés{% endblock %}

{% block body %}
<div class="card">
    <div class="card-header sticky-top top-nav text-bg-light">
        <h1>Libellés</h1>
        <nav class="nav bg-body-tertiary nav-pills">
            <div class="nav-link ms-auto">
                <div {{ vue_component('SearchFilter') }}></div>
            </div>
        </nav>
    </div>
    <div class="card-body">
        <table  class="table table-striped table-sm align-middle">
            <thead>
                <tr>
                    <th>Nom</th>
                    <th>actions</th>
                </tr>
            </thead>
            <tbody {{ vue_component('Labels') }}></tbody>
        </table>
    </div>
</div>   
{% endblock %}
