{% extends 'base.html.twig' %}

{% block title %}{{ account.name }}{% endblock %}

{% block body %}

    <div class="card">
        <div class="card-header sticky-top top-nav text-bg-light">
            <div {{ vue_component('Account', { 'id': account.id }) }}></div>
            <div {{ vue_component('SerachFilter', { 'account': account.id }) }}></div>
        </div>
        <div class="card-body">
            <table class="table table-striped table-sm table-responsive mt-2">
                <thead>
                    <tr>
                        <th>Date</th>
                        <th>Libélé</th>
                        <th  class="text-end">Montant</th>
                        <th></th>
                        <th></th>
                    </tr>
                </thead>
                <tbody {{ vue_component('TransactionsImport', {'account': account.id, 'filename': filename}) }}></tbody>
            </table>
        </div>
    </div>

{% endblock %}
